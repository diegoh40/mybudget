<nav class="navbar">
  <%= link_to image_tag("back-icon.png"), back_path, class: 'back-btn' %>
  <h3 class="nav_heading"><%= heading %></h3>
  <%if user_signed_in?%>
    <%= button_to "Sign out", destroy_user_session_path, method: :delete, class: 'sign_out_btn' %>
  <% else %>
    <%= link_to "", class: "empty"  %>
  <% end %>
</nav>